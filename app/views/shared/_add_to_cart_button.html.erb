<%= button_to(
      current_cart&.products&.include?(product) ? "Added to cart" : "Add to cart",
      cart_items_path(product_id: product.id),
      {
        id: "add_to_cart_button",
        data: { turbo_submits_with: "Adding..." },
        disabled: current_cart&.products&.include?(product),
        class: "pt-3 pb-2 bg-primary text-white w-full mt-2 rounded-sm font-primary font-semibold text-xl flex justify-center items-baseline hover:bg-dark cursor-pointer disabled:opacity-50 disabled:cursor-not-allowed",
        "aria-label": "cart-button"
      }
    ) 
%>
